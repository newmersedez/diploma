<Window x:Class="Diploma.Client.MVVM.View.RegistrationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:Diploma.Client.MVVM.View"
        xmlns:viewModel="clr-namespace:Diploma.Client.MVVM.ViewModel"
        mc:Ignorable="d"
        Height="400" Width="400"
        Background="#2F3136"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResizeWithGrip"
        BorderThickness="1"
        BorderBrush="Gray">
    <Window.DataContext>
        <viewModel:RegistrationViewModel />
    </Window.DataContext>
    
    <Grid ShowGridLines="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition />
        </Grid.RowDefinitions>
        
        <Border Grid.Row="0"
                Grid.Column="0"
                Background="#252525"
                MouseDown="WindowBorderOnMouseDown">
            <Grid HorizontalAlignment="Stretch">
                <Label Content="Авторизация"
                       Foreground="Gray"
                       FontWeight="Bold"
                       FontSize="15"
                       Margin="8 0 0 0"/>
                <StackPanel HorizontalAlignment="Right"
                            Orientation="Horizontal">
                    <Button Width="35"
                            Height="35"
                            Content="___"
                            Foreground="Gray"
                            HorizontalAlignment="Right"
                            Background="Transparent"
                            BorderThickness="0"
                            Click="MinimizeButtonOnClick"/>
                    <Button Width="35"
                            Height="35"
                            Content="▢"
                            Foreground="Gray"
                            HorizontalAlignment="Right"
                            Background="Transparent"
                            BorderThickness="0"
                            Click="MaximizeButtonOnClick"/>
                    <Button Width="35"
                            Height="35"
                            Content="⨉"
                            Foreground="Gray"
                            HorizontalAlignment="Right"
                            Background="Transparent"
                            BorderThickness="0"
                            Click="CloseButtonOnClick"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <StackPanel Grid.Row="1">
            <Label Content="Имя"
                   Foreground="DarkGray"
                   FontWeight="Bold"
                   FontSize="15"
                   Margin="8 0 0 0"/>
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
            <Label Content="Email"
                   Foreground="DarkGray"
                   FontWeight="Bold"
                   FontSize="15"
                   Margin="8 0 0 0"/>
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />
            <Label Content="Пароль"
                   Foreground="DarkGray"
                   FontWeight="Bold"
                   FontSize="15"
                   Margin="8 0 0 0"/>
            <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Зарегистрироваться" 
                    Command="{Binding RegisterCommand}"/>
            <TextBlock Text="{Binding Error}"
                       Foreground="Red"/>
            <TextBlock Text="Уже есть аккаунт?" 
                       Foreground="DarkGray"/>
            <Button Content="Войти"
                    Background="Transparent"
                    Foreground="DarkGray"
                    BorderThickness="0"/>
            
        </StackPanel>
        
    </Grid>
</Window>
